<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tela cheia ocupando também a área do recorte da câmera (sem faixa preta no topo). -->
    <style name="Theme.RNFE" parent="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
