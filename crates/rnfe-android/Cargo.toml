[package]
name = "rnfe-android"
description = "RNFE no Android: biblioteca nativa (NativeActivity) carregada pelo app em android/."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "rnfe_android"
crate-type = ["cdylib"]

[target.'cfg(target_os = "android")'.dependencies]
# sem gilrs: não há backend de gamepad do gilrs no Android (o winit já entrega os botões)
rnfe-gui = { path = "../rnfe-gui", default-features = false }
rnfe-frontend.workspace = true
log.workspace = true
winit = { workspace = true, features = ["android-native-activity"] }
jni = "0.21"
android_logger = "0.14"

[lints]
workspace = true
