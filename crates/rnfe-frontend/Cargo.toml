[package]
name = "rnfe-frontend"
description = "Peças comuns a todo frontend do RNFE: cadência de frames, estado de input. Sem dependências."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["state"]
# Save states e rewind (liga `serde` no núcleo).
state = ["rnfe-core/serde"]

[dependencies]
rnfe-core.workspace = true

[lints]
workspace = true
